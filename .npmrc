# Project-specific pnpm configuration.
# Build script allowances (needed for Prisma + Next.js tooling)
allow-build-scripts=prisma,@prisma/engines,sharp,unrs-resolver

# Prevent pnpm from trying to purge node_modules in non-interactive environments (Docker/CI)
confirm-modules-purge=false
